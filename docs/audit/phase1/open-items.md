# Phase 1 遗留与受限事项（第二轮收尾后更新）

状态标记：✅ 已修复 · 🔎 已验证不构成问题 · 🟥 仍阻塞/待授权 · 📝 产品级观察（记录，后续轮次）

## 第一轮受限项处置结论

| # | 项 | 结论 |
|---|---|---|
| 1 | 线上验证（SHA/登录/写操作/付费调用） | 🟥 仍阻塞——线上环境与授权不在本轮范围；未执行任何线上操作 |
| 2 | 浏览器 E2E | ✅ 本轮已完成（合成环境完整课堂链路 + 恢复 + 降级，见 acceptance-integration.md 第三节） |
| 3 | 真实设备（真麦克风/触控） | 🟥 仍阻塞——需真人真机；本轮窄屏仅响应式模拟 |
| 4 | 人工无障碍 | 🟥 仍阻塞——需真人评估 |
| 5 | 真实课堂与恢复演练 | 🟥 真实课堂阻塞；**隔离回滚演练已完成**（T4 revert 演练 PASS）；生产备份/恢复演练仍需独立授权 |
| 6 | auth.py grant_file 授权改派 | ✅ **已修复**（1fa6967：ON CONFLICT DO NOTHING 首授即定；红绿回归覆盖管理员预览不改派、教师保留、重复授权稳定） |
| 7 | AI/语音调用并发准入 | ✅ LLM 出站公共入口闸已落地（2c59e11，每实例/单进程语义）；语音会话并发上限在第一轮 T3 已落地；**部署前提：多进程需按进程配置限额** |
| 8 | api.ts/types.ts 类型收尾 | ✅ 已完成（1f97780：UiCapabilities 白名单类型 + fetchUiCapabilities；HealthResponse 收窄为真实存活载荷；无 any、无双实现） |
| 9 | uvicorn access log 记录 legacy token | ✅ **已修复**（d37fc93：控制组实测泄露→QueryStrippingAccessFormatter + log-config 接入启动脚本→哨兵复验 0 次落盘、日志仍完整） |
| 10 | 反代/Cookie Secure/进程数配置 | 🟥 待运维信息（本轮无新输入） |
| 11 | 私有题库逐项正确性/来源许可 | 🟥 待内容所有者；导入/预览/授权链路本身已验证 |
| 12 | 主检出未提交条目内容 | 🔎 仅记录条目名（集成所有者事务），未触碰 |

## 第二轮新增验证结论

| 项 | 结论 |
|---|---|
| 标准 pytest 入口数据隔离 | ✅ 已修复（3a161e1 根 conftest 守卫；此前实证标准入口会打开当前检出的 backend/data 实例） |
| 同教师双项目串用 | 🔎 已验证不构成问题（T2 边界 + 新回归固定） |
| 管理员预览改变文件归属 | ✅ 已修复（见 #6） |
| 历史授权行绕过新边界 | 🔎 设计确认：历史行继续有效（兼容；禁止清洗生产表）；新授权已被允许根收口 |
| 语音时长限制在等待期失效 | ✅ 已修复（2f380d5：wait_for 双预算 + 空闲超时） |
| 超限/永久错误无限重连 | 🔎 已验证不构成问题（有界 3 次退避仅用于传输错误；4429/4407 终态不自动重连） |
| QGIS 名额误释放 | 🔎 已验证不构成问题（名额绑定底层步骤生命周期，C5 三态测试固定） |
| DATASET_NOT_ALLOWED/QGIS_BUSY 被降级为 INTERNAL_ERROR | ✅ 已修复（ef45f8f 补错误码白名单） |
| 真实 QGIS 四工作流 | ✅ REAL_QGIS_PASS（D:\QGIS 3.40.10；第一轮 NOT_RUN 系过时路径假设，已纠正） |
| 报告 evidence_refs 可定位性 / real 免责 / 重复发起 / 载荷信息量 | ✅ 已补齐并测试固定（f368df3） |

## 产品级观察（📝 不在本轮修复范围）

1. 数据库面板向**已登录资源所有者**展示服务器绝对路径（本地优先产品取向；非公开接口泄露，与 T3 收窄的 /health 不同）。后续可改为相对标签。
2. 投屏层秒级计时重渲染影响自动化工具 actionability（功能正常；自动化友好性问题）。
3. 超长会话的计时 UI 直显墙钟分钟数（诚实但刺眼；报告侧语义已正确，UI 可后续加"含挂机时段"提示）。
4. 前端 RehearsalPanel 测试在满负载并行下出现过一次时序性失败（单跑稳定通过；非本轮改动引入；PR 说明中如实标注为已知 flake，不以删断言/扩 mock 换绿）。

## 仍阻塞清单（进入 PR 评审不依赖，合并/部署前需人工决策）

- 真人语音、真机触控、真实教师课堂、线上验收（#1/#3/#4/#5）
- 运维配置核对（#10）
- 生产备份/恢复演练（需独立授权）
