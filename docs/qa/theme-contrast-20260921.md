# 深浅主题可读性核查（2026-09-21）

## 范围与起点

分支 `codex/theme-contrast-20260921`，起点 `9e77fc392b9159d7b8c5c0e4f8f1f6851c54e1f9`。
独立工作树仅调整前端颜色、浏览器标题和 favicon；允许路径为本次涉及的七个 CSS 文件、
`frontend/index.html`、`frontend/public/favicon.svg` 和本记录。验收依赖现有课堂流程与隔离预览。
业务逻辑、地图数据配色、生产课堂记录、账号权限、原教案及其他工作树内容未修改。

## 修复

- 深色界面从接近黑色的蓝底提亮为石板蓝，基础面板 `#253749`，抬升面板 `#30465b`。
- 倒计时、超时、答案、报告统计、题库计数、教案工作台及输入框使用随主题切换的前景和背景。
- 助教、导入反馈、登录注册、账号面板等残留固定浅色/深色文字改用语义色；弱提示和禁用标签仍保持可读。
- TOP20 条形在深色模式增加细轮廓，原始数据色不变。
- 浏览器标题改为“地理智能教学平台”，新增蓝底白色 G 的 SVG favicon；页面原有 GeoBot 品牌保持。

## 实页核验

使用隔离的 `127.0.0.1:5191` 前端与 `127.0.0.1:19091` 后端、临时数据目录和测试账号。
关闭预览中的外部模型密钥；使用内置上海课例与规则报告，不写入生产课堂。

| 页面/操作 | 实际核验结果 |
| --- | --- |
| 3D 地球、2D 地图和主界面 | 正常加载，深浅主题面板及工具可辨识 |
| 投屏题目 | 开始、暂停、继续、自然超时、揭晓答案、关闭均完成；倒计时及答案深浅两色可读 |
| 页面恢复 | 主题切换跨标签同步，刷新后课堂/投屏状态恢复 |
| 课后报告 | 实际结束课堂并生成规则报告，分钟数、题数、0 人和“未采集”清楚可见 |
| 教案工作台 | 两种主题查看草稿、展开直接编辑；正文、步骤编号、提示和操作可读 |
| 资料库 | 两种主题查看分类计数、列表及操作；消除深色选中计数的双层底色 |
| 助教与 GIS 菜单 | 两色检查会话、图片入口的未配置/禁用状态及工作流菜单；未发起付费生成 |
| TOP20 查询 | 实际查询 2020 年常住人口最多的 20 个地级市，生成图层与排名，核验两色名称、数值 |
| 登录/注册与账号管理 | 两色实页核验标题、占位符、表单提示、账号邮箱及操作；未修改账号权限或密码 |
| 网页标题与图标 | DOM 标题正确，favicon 指向 `/favicon.svg`，浏览器打开并目视确认白色 G |

在可见 DOM 文本上读取最终计算颜色，叠加纯色祖先背景并按相对亮度抽查。
普通文字阈值 4.5:1、大字 3:1；最终抽样中未发现低于对应阈值的纯色文本。
代表性最小比值：投屏答案深色 4.93、浅色 4.70；资料库深色 4.94、浅色 5.14；
报告深色 5.63、浅色 5.65；教案工作台两色 5.17；TOP20 深色 5.63、浅色 5.13；
登录和注册深色 5.16、浅色 8.25。
这些是已打开状态的抽样，渐变、地图像素、透明合成与动画用截图目视复核，不代表完整 WCAG 认证。
导入反馈的成功/失败配色经过源码检查及现有组件测试，未重新上传生产资料。

## 自动验证

- `npm test`：60 个文件，430 项通过，31.57 秒。
- `npm run build`：TypeScript 和 Vite 构建成功，458 个模块，6.87 秒。
- `git diff --check`：通过。
- 已有打包体积大于 500 kB 的提示仍存在，不影响本轮构建。
- 本轮没有后端源码改动；PR 的完整 CI 结果及最终发布提交以 GitHub 和运行实例记录为准。

本次核验不声称完成真实课堂、手机实机、麦克风或外部模型服务验证。
