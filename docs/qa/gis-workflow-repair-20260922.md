# GIS 实战修复与验收（2026-09-22）

## 范围与来源

分支 `codex/gis-workflow-fixes-20260922`，起点 `df3d5f03042a90ba313fb5951dc5a8fa482ae7e4`。在独立工作树开发，共享入口文件串行修改。ZCode 课堂任务工作树为干净状态，既有接管报告确认该批工作完成。桌面未跟踪资料、旧导入数据及错误产物全部保留。

公开样本见 `backend/tests/fixtures/gis/README.md`，35 个原始 USGS 能登地震点，无 `name` 别名。所有离线检查使用独立临时数据目录。

## 修复前后

| 项目 | 修复前实测 | 当前实现与离线证据 |
|---|---|---|
| 20 km 缓冲 | 实际约 15.728–15.979 km，使用墨卡托平面距离 | 点/多点使用 WGS84 椭球投影测地圆，能登结果 19.975845–20.000000 km |
| 原始字段 | 缺少 `name` 时失败 | 自动选 `name` / `place` / `title`，否则使用要素编号 |
| 震级分级 | 忽略文字，错误使用 `population` | 服务端解析 `mag / equal / 3 / mag_class`；35 点分档 29、5、1 |
| 统计解释 | 20 条示例被描述为总数 | 汇总保留 35；界面区分总数和示例；数量、距离、边界由代码生成 |
| 下载与保存 | 重新登记会更换产物 ID，解释生成后未同步 | 按工作流/类型/路径更新；完成后再次登记解释；完成事件刷新数据库 |
| 重复上图 | 每次创建新图层，样式无法完整恢复 | 复用同一产物图层，恢复可见性、逐要素颜色及图例，定位当前二维/三维视图 |
| 投影输入 | QGIS 导出 GeoJSON 未真正转换坐标 | 写出时显式指定坐标变换，避免米坐标被当作经纬度 |

## 参数接口

`POST /workflow/preview` 与 `/workflow/submit` 共用解析与校验，前者不创建任务。请求沿用 `project_id`、`template_id`、`message`、`parameters`，结果返回 `fields`、`parameters`、`parameter_sources`、`issues` 和 `valid`。优先级：手动覆盖、明确文字、模板默认值。项目访问权限和数据路径边界沿用原规则。

缓冲参数为米制距离、是否合并、标签字段；前端可切换米/公里。分级参数为数值字段、方法、2–12 级和新输出字段。空值、非数值与非有限数不进入任何有效等级。区间左闭右开，末档包含最大值。上传数据缺少目标字段时要求选择，不改用人口字段。

## 真实 QGIS 独立量算

命令：Python 3.12 运行 `scripts/qa/check_gis_workflow_quality.py`，QGIS 3.40.10。使用独立的 `pyproj.Geod(WGS84)`、Shapely 验证，而非调用被测缓冲代码计算期望值。检查边界顶点及每段 1/4、1/2、3/4 位置，并检查全部输入点被覆盖。

| 检查 | 最大误差 | 结果 |
|---|---:|---|
| 赤道单点 | 0.120455% | 通过 |
| 37°N 单点 | 0.120765% | 通过 |
| 60°N 单点 | 0.121168% | 通过 |
| EPSG:3857 投影输入 | 0.120765% | 通过 |
| 重叠点合并/不合并、多点 | 0.120766% | 通过，合并 1 要素、不合并 2 要素 |
| 能登原始 35 点合并 | 0.120775% | 通过，35/35 点覆盖 |

同时验证线/面局部等距投影结果几何有效、等距分级边界 `[4.5,5.5,6.5,7.5]`、空值/文本/中文字段混合输入。共 12 个真实 QGIS 检查通过。线/面在要素中心距离加缓冲半径不超过 500 km 范围工作；极区及跨日期变更线情形明确拒绝。

## 自动回归与发布验收

后端标准全量回归 **966 通过、6 跳过、156 个子测试通过**（JUnit 文件保存于本次工作树的 `.claude/qa/full-backend-final.xml`）。前端全量 **460 通过**，TypeScript 与正式域名配置的 Vite 构建通过；`git diff --check` 通过。发布后的真实任务编号与运行结果将追加在本报告中。

发布前须完成完整后端回归及停机数据备份；发布后通过 Computer Use 重新导入、提交两例、检查图例/统计/下载、刷新后重复上图，并对运行产物独立量算。离线结果不冒充正式站验收。
