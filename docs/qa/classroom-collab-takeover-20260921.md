# PR #28 中断任务接管（2026-09-21）

目标：完成原协作 PR 的中断事项，保留原实现与运行记录，在当前课堂版本上复验实际问题。

- 工作分支 `codex/finish-classroom-collab-20260921`，起点 `e2d439a239a6129929588bdbe23265f73cf6a422`。
- 原 PR 分支 `codex/classroom-collab@4a4163b`；原 ZCode 工作树仍为 `c99c32f`，无遗留实现提交或未提交修改。
- 本轮先保存接管提交 `3f4a1e4`，再合入已完成 PR #32 的 main `e425d5d`，无冲突。
- 允许范围：原 `scripts/teaching_collab/` 及测试，ReportPanel、LessonDesignWorkspace、ClassRunPanel、classroom_workflow、lesson_design；浏览器发现 TOP20 路由阻塞后，将 assistant 的对应规则及 visual_query 回归纳入。另更新相关验收文档。
- 禁止范围：原教案、生产库、原队列与 MiMo/ZCode 工作树、其他无关文件；本轮均未修改。

## 原中断原因与修复

只读查询原队列 `batch_c386e9d18809`：六项连接探针及第一轮两次讨论已有记录，第二次专家结果为 `accept` 但 `issues: []`，教师结果仍确认三项问题。协调器使用空列表跳过实现进入第二轮，原批次随后被暂停。

现在空列表的接受评审会被拒绝，当前租约保留，可提交正确列表；教师确认若附带问题，内容必须与评审一致。回归复现第二轮讨论场景，确认修正结果进入 implement，不直接进入下一轮。未补写客户端完成记录，未重启旧客户端或运行旧批次。

独立预览固定 `.claude/collab-preview-data` 和其中的账号库，防止继承生产数据环境变量；标准测试的数据保护保持有效，不自动删除已有实例。

## 三项课堂问题

| 原问题 | 当前处理 | 实际结果 |
|---|---|---|
| 历史课堂难以找回 | 复盘页保留当前项目默认列表，增加“查看全部可访问课堂”，列出项目名和结束状态 | 浏览器从当前项目切到另一个隔离示例项目的已结束课堂并生成规则报告；服务端仍按当前账号授权过滤，普通教师看不到他人的课堂 |
| 空白草稿标题被按钮文字污染 | 前端空需求提示补充课题，后端不再把“生成完整初稿”当作需求正文；已有课题仍可复用 | 浏览器空白草稿点击按钮显示明确提示，标题保持未命名；测试验证未调用模型、未改草稿 |
| TOP20 无课堂入口 | 人口课小结增加默认收起的选用拓展，标明人口总量不等于密度，复用既有指标查询 | 浏览器先发现附带的 query_features 缺少 layer_id；修复规则互斥后，只执行 run_visual_query，实际生成 20 城市柱状图与地图高亮，年份 2020，第一项重庆市 32054159 人 |

## 验证

- 后端完整组合回归：Python 3.12 + 本机 QGIS 3.40.10，**934 passed, 2 skipped, 152 subtests passed**，352.05 秒。
- 最后补充 TOP20 路由修复后，visual_query / planner_coverage / image_generation / teaching_context：**54 passed, 41 subtests passed**，15.57 秒。原有普通图层查询与图片生成规则仍通过。
- 前端完整：**60 files / 430 tests passed**，18.14 秒；正式域名生产构建成功，仅既有大包提醒。
- 浏览器：空白草稿保护、课堂小结默认收起拓展、真实 TOP20 查询、结束课堂、跨项目历史选择、规则报告均完成。没有回答的报告显示“未采集”，没有生成学生正确率或掌握等级。
- Git 中断标记核对覆盖全部登记工作树：仅发现本轮正在完成的主线合并，没有其他遗留 merge/rebase/cherry-pick/sequencer/index.lock。
- GitHub 精确提交 CI、最终合并和生产同步将在此验收后执行；最终运行代码与资源指纹见发布实例 `backend/data/public-runtime/release.json`。

浏览器数据均为隔离演练，不是真实学生课堂，也不能证明 40 分钟教学效果。没有启用新付费模型调用。旧队列保留暂停时的原始事实，本轮完成结果以接管 PR 和本记录为准。
