# PR #32 接管验收（2026-09-21）

目标：在已经发布的人口分布课堂版本上完成安全 PR 的兼容集成，并补齐本轮复查发现的缺口。

- 工作分支：`codex/finish-pending-prs-20260921`；起点 `e2d439a239a6129929588bdbe23265f73cf6a422`。
- 接管对象：`claude/phase1-integration-review@62a34021435afa799da0746892bbc7d177247bb6`，PR #32；无冲突合并。
- 范围：PR 原有后端权限、资源预算、报告证据和前端能力接口、语音终态文件及其测试；另补本文。未改原始教案、生产数据、其他工作树或无关未跟踪文件。
- 保留 `/health` 最小探针、`/ui/capabilities` 脱敏能力集、管理员诊断、项目文件边界、上传/JSON/LLM/QGIS/语音预算和报告证据口径。

## 本轮补齐

1. 简写数据路径同样检查所属上传目录和内置目录的解析后包含关系，封住 `../` 跨项目路径；预检查与 QGIS worker 同步执行。
2. 应用加载时安装 Uvicorn 请求日志过滤，覆盖未传 `--log-config` 的正式启动器和 WebSocket 握手日志；保留方法、路径、状态码等访问记录。
3. 语音达到会话时长、空闲或帧大小上限后停止自动重连，仍可手动重试。
4. 修正文档中无条件删除 `backend/data` 的指引；只在确认隔离工作树内的测试数据后归档，保护桌面、上线数据及 Junction。

## 当前验证

- Python 3.12，设置本机 QGIS 3.40.10：后端完整测试 **896 passed, 2 skipped, 152 subtests passed**，341.88 秒。
- 前端完整基线：**60 files / 424 tests passed**。补充语音预算终态后，voiceSession + voiceStream **22 tests passed**（含 3 个新增场景）；正式域名生产构建成功。既有大 chunk 提示仍在。
- 独立回环预览：`/health` 仅含 status，capabilities 可加载；浏览器完成开课、切至中国观察、自主绘线步骤、说明依据、明确揭示参考线。初次观察没有参考线，地图、图例、导航和教师控制正常。
- 真实 Uvicorn 默认启动（未指定日志配置）：使用非密钥哨兵查询参数访问 HTTP 与被拒绝的 WebSocket；日志仅出现无查询串路径，未出现哨兵。
- 第一轮课堂发布已完成：桌面与 GitHub main 为 `e2d439a`；正式运行工作树为合并提交 `0263f2c`，代码树完全相同。发布前静态备份目录 `20260921-144139-75405e9c8efe`，287 文件，307703374 字节。正式域名校验 12 项、0 失败，代理 DNS 提示 1 项。
- 正式站新建的验收课堂已结束。保存记录明确标为“预设示例，非学生参与”，课后可见；没有对历史课堂作批量修改。

## 证据边界

浏览器验收为操作演练，不是 40 分钟真实课堂。未测试真实学生、麦克风输入或手机蜂窝网络；没有本轮付费模型调用证据。用户确认继续采用原教案对应的人教版必修二，2025 修订要求的学校适用情况仍待教师核对。实际最终发布提交与 PR 状态以之后的发布记录为准。
