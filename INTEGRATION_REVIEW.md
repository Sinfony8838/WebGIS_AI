# 分支集成与交接问题复验（2026-09-08）

> 历史快照：本文只记录 2026-09-08 当次集成与运行复验。提交、测试数量、端口、进程和外部服务状态均具有时效性，不代表当前 `main`；需要当前结论时必须重新获取远端并复验。

## 范围与来源

集成分支 `codex/integrated-release`，起点为实时核对的 `origin/main=a0491a1c1b029617b1f9161e9ee00446f415f2bb`。独立工作区 `.claude/worktrees/codex-integrated-release`。目标是保留现有功能并接入已完成分支，修复交接中的明确问题。

允许范围：下列分支既有变更、重叠处的适配、状态保存、资源所有权、分类回放、现用服务环境恢复及相应测试和说明。禁止覆盖其他工作区未提交源码、认证数据和私有资产；不引入新运行依赖、不修改数据库表结构、不设计新对外接口。测试依赖单独固定至用户已批准版本。

| 来源 | 提交 | 集成处理 |
| --- | --- | --- |
| #9 保守维护 | a68cbdf | 语音尾句、资源释放、SSE 与初始快照竞态 |
| #8 数据库 | c9c5252 | 保留新版分类与产物功能，接入当前智能交互样式 |
| zcode 制图交接 | bb3d14b | 保留样式、回放、缺图处理、外置大要素；未恢复旧数据库组件 |
| #10 主题 | a854d12 | 主题切换、初始化取消保护与题库加载共同保留 |
| #11 交互审计 | fe37169 | 完整指令规划、状态同步、保存优化，与外置要素组合 |
| Harness | 934bdb0 | 适配现有 26 个工具契约和 21 个交互工具；课堂权限隔离继续生效 |
| #12 盘点 | 46ad9e1 | 保存历史来源与补丁等价证据 |

图片、旧 P0 持久化等补丁等价分支不重复合并。旧发布分支只作历史保留，不覆盖新 UI。各来源使用合并提交保留祖先关系，便于验证包含性；不 force push、不删除原分支。集成前回退点：`backup/pre-integration-20260908`（a0491a1）。

## P1 延迟归因

复制 zcode 的 `backend/data/state`（主 JSON 3,230,074 字节及外置要素文件）到独立临时目录，运行同一个真实 QGIS 人口密度分级设色工作流。两组使用同一集成执行器，只替换 RuntimeStore 为 zcode 提交或集成实现。关闭可选模型摘要，执行线程完整结束后计时，避免将请求返回或提前写入 success 当作完整完成。每组一次冷启动、两次热运行。

| 实现 | 冷运行 | 热运行 1 | 热运行 2 | 热运行保存耗时 |
| --- | ---: | ---: | ---: | ---: |
| zcode store | 45.527 s | 25.719 s | 25.876 s | 25.400 / 25.542 s |
| 集成 store | 4.476 s | 2.873 s | 2.880 s | 2.552 / 2.559 s |

六次均真实执行成功。热运行均值下降约 89%。旧保存路径在外置要素之前执行 `ProjectRecord.to_dict()` → `dataclasses.asdict()`，仍遍历所有坐标；cProfile 将主要 CPU 时间定位于 `_asdict_inner`。因此不能认定状态保存已排除，也没有依据改 Defender 排除项或系统安全设置。

旧 store 不含数据库分支的产物登记兼容，保存次数为 14；集成版完整登记产物，保存次数为 23。上述比较不是两个整分支的速度排名，也不是不同数据量或模型服务耗时的承诺。没有恢复到早期空数据状态的 0.61 s；真实数据量和历史记录仍影响 JSON 保存成本。

修复保留外置格式与旧内联格式读取能力。只复制项目/图层/数据包装字典，不递归拷贝大要素；重启复用已有文件；删除后复用同一图层 ID 不再命中旧缓存；外置文件原子写入、稳定摘要命名，并限制恢复引用不得逃出目录。

## P2 浏览器补验

独立 Chrome、1600×1000、实际前后端 5182/18995，使用本机软件 WebGL。九项逐个操作、截图并目检：世界国家边界、世界主要城市、世界主要港口、胡焕庸线、中国主要河流、地形三级阶梯、自然植被带、上海区县边界、省级人均 GDP。点、线、面正常显示，未出现页面 JavaScript 异常。

原交接标题写“8 个”，列举实际为 9 个，本轮按九个全部检查。截图位于本工作区 `.claude/layer-1.png` 至 `layer-9.png`，对应信息为 `.claude/layers-qa.json`；这些是本地验收产物，不提交仓库。示意数据仍保留“示意”标识；不把渲染成功当作数据精度认证。

## 额外明确修复

- `/projects` 与 `/projects/{id}` 原本已省略 features；现在在复制前就排除 data，输出结构不变。
- 检索收藏仅复用当前所有者条目，并生成独立条目 ID；不再跨用户写同名集合；无来源字段时保留摘要。
- GeoJSON 产物上图依据真实几何类型，不再统一标为 Polygon。
- 字段分级样式按分类编号匹配颜色，图例保留原值范围；点结果在回放中保持可见；减少动态效果时不保留已完成的动画句柄。
- 验收脚本移除写死账号，PNG 按二进制签名检查，产物读取错误会令矩阵失败。
- 数据库图片继续内嵌预览，非图片产物恢复原有的新窗口打开方式，避免把文档作为图片加载。

## 验证

- Python 3.12：`python -X utf8 -m pytest backend/tests -q --durations=5`，462 passed、9 subtests passed，50.97 s。
- 前端：`npm test`，40 files、245 passed；最终数据库修复后重跑仍全部通过，`npm run build` 成功（3.65 s）。
- 新增状态恢复、缓存失效、禁止深拷贝、引用路径、所有者隔离、分类颜色与点动画回归。
- 七个工作流模板真实 API 最终复测 7/7 成功；PNG、GeoJSON、样式、统计文件均读取成功。
- 浏览器八条交互指令、结束课堂确认与复盘选择链均通过，无页面 JavaScript 异常；覆盖 2D/3D 切换、图层面板、数据库、开课、下一环节、Hu 线工作流、结束课堂。
- PR #13 的 GitHub Actions 后端与前端门禁在 81ed034 上均成功；合并前必须再次确认最终提交的检查状态。
- 真实分类产物的 34/34 个要素各匹配一个颜色区间；合成产物浏览器验证文档打开 URL、图片保留内嵌预览。

## P4 现用服务恢复

原 18999 的健康端点确认 `qgis_root` 为空，且来自旧 question-bank-lesson-flow 工作区。确认没有运行中的工作后，停止原进程，备份运行数据并逐文件校验 SHA-256（50 个文件）。恢复点为主仓库 `.claude/recovery/pre-live-20260908/data`，未上传版本库。

独立发布目录 `.claude/worktrees/codex-live-release` 使用本轮集成源码；`backend/data` 是指向原工作区数据目录的 Windows Junction，保留绝对产物路径和账号。不要删除该原数据目录或把验收数据覆盖进去。源码仍以 main 为准，发布目录仅作运行用途。

18999 后端以 `QGIS_ROOT=D:\QGIS 3.40.10` 和原 users 认证模式重启，5179 前端指向该后端。健康检查成功、QGIS 路径正确、MiniMax 配置存在、前端 HTTP 200；原项目标识集合与认证数据库摘要在切换后保持一致。没有读取密码或在用户项目中提交验收工作流。真实 GIS 验证使用隔离的 18995 服务；用户账号的登录和 GIS 操作尚未在本轮复测。

本地启动日志位于发布目录 `.claude/backend-live.*.log` 和 `frontend-live.*.log`。目录联接只是本机运行安排，不作为部署所需的新接口或代码依赖。

## 保留的边界

- Hu 线工作流仍是省域分级图与省级统计；没有实现两侧分区统计，也没有自动加载第二结果图层。现有可视化面板可以手动叠加胡焕庸线；不能将此描述为完成两侧统计。多结果展示与统计口径属于后续明确设计范围。
- 工作流结果仍仅显示在 2D；3D 工作流结果为既定后续阶段。本轮不改整体架构或新增 3D 需求。
- 三维配色、取景和动效的课堂适用性仍需教师在实际投影设备评估；浏览器画面不代表所有 GPU 与显示环境。
- `/layers` 仍返回完整图层数据；进一步按需传输需单独讨论接口兼容，未在本轮引入新接口。
- 私有教学图片不入库；真实麦克风授权、录音和外部模型服务不由单元测试证明。

其他工作区的未提交源码和原分支未修改。用户数据完整保留在原目录，先备份后由新服务正常使用；验收项目均位于独立测试数据目录。
