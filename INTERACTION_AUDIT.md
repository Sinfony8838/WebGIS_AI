# 智能交互实测与保守修复（2026-09-08）

基线：`a0491a1c1b029617b1f9161e9ee00446f415f2bb`，分支 `codex/interaction-audit`。
工作区为独立 worktree；测试使用独立前端 5181、后端 18994 和合成课堂。
真实模型调用已获得用户授权；未读取教师凭据、录制麦克风或操作其他工作区的服务。

## 结论与边界

智能交互具备规则快通道、一次性 MiniMax 工具规划、执行校验和高风险确认。
可以执行单步命令以及部分彼此独立的多步组合；不能据此认定具备可靠的自主规划闭环。
`AssistantSessionEngine` 一次规划后依次执行，未将工具输出交回模型观察、纠错并再次规划。
`LLMPlanner._interaction_context` 没有传入会话原始消息或任务记忆；跨轮指代实测未完成。

以下结果必须分开理解：任务返回 completed、工具产生结果、用户业务目标完整达成。
例如胡焕庸线模板能正常运行，但其统计含义不足；查询 Top-5 能产生结果，但没有继续定位第一名。

## 问题、证据与修改范围

| 问题 | 实际依据 | 本次修改 |
| --- | --- | --- |
| 复合命令只执行首个关键词；否定句可能反向执行 | “二维，再打开图层”只切视图；“不要结束，只打开图层”命中结束课堂 | 复杂句交给现有规划器；单纯禁止操作直接保持状态；增加回归 |
| 模型知道工具但执行器禁止使用 | schema 声明 interaction 的 13 个共享工具未在 registry 开放该模式 | 仅对齐已声明的可见性；教学专用工具继续隔离 |
| 未知图层误用活动图层 | opacity 规则和运行时目标解析均有兜底 | 明确指定但未匹配的名称不再改活动图层；保留“当前图层”等正常用法 |
| 模型参数与实际目录不一致 | satellite 被默认底图回退；snake_case 样式没有被前端读取；city 被查询服务拒绝 | 提供真实底图 ID、现有样式键、现有查询参数；工具分发拒绝未知底图，不更改底图 HTTP 接口 |
| 模型澄清被丢弃、回复夸大成功 | 空 actions 被校验器拒绝；回复沿用执行前文案 | interaction 接受有正文的空计划；操作后使用执行器实际回复 |
| 已结束的课堂显示 blocked | 确认执行后再次校验“课堂必须运行中” | 复用执行前已通过的校验结果，确认后回复实际结果 |
| 前后端课堂、工作流状态脱节 | 返回 class_session/stage/workflow_id，但面板不消费 | 将已有任务结果同步到课堂和工作流组件；不新增对外字段 |
| 初始化循环和不必要刷新 | 订阅回调随项目状态改变导致初始化 effect 重跑；一次浏览器初始化出现 99 次 health 请求 | 修正依赖；纯视图/面板操作不再等待无关数据刷新；刷新失败仍反馈已执行结果 |
| SSE 固定延迟、保存全状态反复深拷贝 | 固定 sleep(1)；单次指令 14 次保存，cProfile 大量时间在 asdict/deepcopy | 状态变更条件通知；锁内直接序列化 dataclass，保留 JSON 内容、原子写入和保存时机 |

仅修改交互规划/执行、持久化热点、既有前端状态接线、对应测试与审计文档/脚本。
未修改数据库结构、HTTP 接口、依赖、页面布局和技术栈，未补建自主 Agent 架构。

## 全部 8 条界面预设

命令直接取自 `frontend/src/components/CopilotWidget.tsx`。浏览器检查页面状态，后端检查工具结果。

| 预设 | 验收结果 |
| --- | --- |
| 切换到三维地球 | 切到 3D，切换按钮变为返回 2D |
| 切换到二维平面地图 | 切到 2D，切换按钮变为返回 3D |
| 打开图层管理器 | 图层面板显示 |
| 打开数据库面板 | 数据库对话框显示 |
| 开始上课 | 创建运行中课堂，课堂面板显示，下一条指令携带 session |
| 进入下一个教学环节 | 后端 stage 更新，页面活动环节同步 |
| 做一个胡焕庸线对比分析 | QGIS 工作流 success、4 个产物，地图/图例/统计显示；**业务语义仅部分满足，见下文** |
| 结束上课 | 先等待确认；确认后 session=ended，复盘面板读取该课堂记录 |

规则词表另逐条检查 257 种组合：2D/3D 别名、全部面板名×开关动词、课堂/环节短语、内置区域别名。
255 条直接命中预期动作；“转到塔里木盆地”“转到塔里木”两条转交模型。
原因是旧语音规则同时附带教学地图叠加（interaction 未开放），本次拒绝半执行此类混合计划。
这 257 条是离线路由检查，不能称为 257 次完整 UI/GIS 验收。21 个工具的模式可见性有测试，非全部工具均做了真实端到端验收。

## 真实 MiniMax 未预设指令

| 输入 | 最终观察 |
| --- | --- |
| 先切换到二维地图，再打开图层管理器 | 两个工具按序执行 |
| 不要结束上课，只打开图层管理器 | 仅打开图层；未结束课堂 |
| 把画面调成适合讲解长江三角洲的范围，并用卫星底图 | set_view + amap_imagery，项目底图确为高德影像 |
| 把地图中心设为东经120度北纬30度，缩放到7级 | view.center=[120,30]，zoom=7 |
| 请把不存在的火星矿产图层调到半透明 | 明确指出图层不存在，零动作，未误改活动图层 |
| 给人口图层换一种醒目的蓝色并将它放到最上面 | fillColor=#3399FF、strokeColor=#0066FF、z_index=100；检查保存状态和前端实际读取的键名，未单独做像素颜色比对 |
| 查询2020年人口最多的20个城市 | 最终重测生成 Top-20，20 项查询结果；修复前模型生成 city 参数导致失败 |
| 找出人口最多的五座城市，然后定位到第一名 | 最终重测仅执行 Top-5 查询，未执行定位；**部分完成** |
| 暂时不用打开两个面板；随后同一会话“现在打开这两个面板” | 最初模型误打开面板，已增加禁止操作保护并实测零动作；跨轮引用仍会询问是哪两个面板，未证明记忆可用 |

这些是有限样本，不代表任意措辞的成功率。真实音频采集、ASR、唤醒、TTS 的端到端延迟未测。
文本和语音识别后的 interaction 规划共用规则；已有语音单元测试通过不等于真实语音已验收。

## 延迟

同机、同一份 4,985,282 字节状态副本、同一条“打开图层管理器”，交替运行基线与新 RuntimeStore 各 3 次。
每次从相同文件建立临时运行目录；计时仅覆盖实际后端任务执行，不含创建项目/初始化、网络、浏览器或模型。

| 保存实现 | 3 次耗时（ms） | 中位数 |
| --- | --- | --- |
| 基线 | 6529.8 / 6620.7 / 6660.1 | 6620.7 ms |
| 优化后 | 1556.2 / 1616.5 / 1557.5 | 1557.5 ms |

本组中位数下降约 76.5%。这是受控后端样本，不是整套 UI 或模型的加速比例；3 次样本不足以给出可靠 p95。
较早较小状态的 cProfile 也显示同一热点，但不与本表混算。

独立浏览器优化后的一轮点击到反馈约 2.1–4.9 秒（8 个预设，每个一次），启动 health 请求从 99 次降至 1 次。
最终一轮加入工作流完成/产物断言，并与后端全量测试有负载重叠，反馈为 4.6–9.1 秒，等待 GIS 产物约 20.7 秒；8 条断言均通过、无 pageerror。该轮用于功能验收，不用于计算性能提升比例。
后来增加多份测试课堂/结果后耗时再次上升，说明全量状态 JSON 的写入开销仍随数据量增长。
真实模型案例约 5–9 秒；查询并尝试后续定位的一次为 13.39 秒，其中包括状态保存。
现有 12 秒模型超时只约束模型请求，不是端到端预算。

没有引入异步持久化、数据库迁移、缓存框架或更换模型；这些超出本次最小改动范围。

## 遗留问题

1. `workflow_templates.py::_template_hu_line_compare` 默认只 load/fix/choropleth/aggregate；没有胡焕庸线几何切分、东西分组或分组汇总。输出是 34 个省级记录的整体统计，不能声称完成“两侧对比”。本次记录缺口，不擅自选择新的分区统计口径。
2. 没有工具结果驱动的重规划；跨轮记忆未接入 interaction prompt。Top-5 后定位第一名仍缺一步。
3. `export_snapshot` 工具声明只有 title，校验器却要求导出路径，运行时也只是提示用户手动截图；不能宣称助教会自动导出截图。现有页面截图功能保留。
4. 两条塔里木区域规则需要模型兜底；未知样式参数、复合否定句和模糊图层名仍需更系统的模型评测，不能以本轮有限样本证明全面可靠。
5. 工作流首次加载与 SSE 更新的竞态已有独立维护 PR #9 涉及；本轮浏览器未复现，未在此重复修改。App 初始化修复与 UI PR #10 有一处重叠，合并时需检查。
6. 使用已安装 QGIS 3.40.10 进行验证。最初手工启动未设置 QGIS_ROOT 的失败属于该测试环境配置问题；项目启动脚本已有 QGIS 探测。
7. 不含真实学生答题数据，也未据测试课堂推断参与率或正确率。未完成真实麦克风、外部 POI 服务和所有 3D 主题场景的端到端验收。

## 复现与验证

仓库常规启动使用 `scripts/start_webgis_ai.ps1`；审计应另建独立 worktree/端口。
测试后端仅在独立进程临时设置 `WEBGIS_AI_AUTH_MODE=disabled`，不要更改共享服务的认证配置。
如运行 QGIS，设置 QGIS_ROOT 与 WEBGIS_AI_QGIS_PYTHON 为本机实际安装路径。
使用已有 MiniMax 配置；不要把密钥写入脚本、结果或 Git。

```powershell
& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -X utf8 -m pytest backend/tests -q
Push-Location frontend
& "C:\Program Files\nodejs\npm.cmd" test
& "C:\Program Files\nodejs\npm.cmd" run build
Pop-Location
git diff --check

# 会创建测试项目和课堂、调用配置好的模型；只用于独立本地测试服务。
& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -X utf8 scripts/audit_interaction.py `
  --base-url http://127.0.0.1:18994 --allow-model-calls --output .claude/audit-new.json
```

脚本保留每条请求、规划、执行结果及操作后项目状态；只有枚举的测试“结束上课”会自动确认。
脚本记录结果供人工验收，不把 completed 自动转换成业务成功；输出文件已存在时拒绝覆盖。
浏览器使用本机已有 Playwright 和 Chrome，未加入项目依赖。

最终验证：后端 437 passed、9 subtests passed（91.79 秒）；前端 35 个测试文件、207 项通过；TypeScript/Vite 构建通过；git diff --check 通过。
原始测试 JSON 和截图留在本任务工作区 .claude 目录中，按仓库约定不提交运行产物。所有修改均限于上述独立工作区；未改动 zcode、主工作区或其他任务的未提交文件。
